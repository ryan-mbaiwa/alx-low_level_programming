#include <stdio.h>

/**
 * main - prints out the name of the program
 * @argc: the legnth of arguments
 * @argv: the array holding the argument
 * Return: always zero
 */

int main(int argc __attribute_((unused))_, char *argv[])
{
printf("%s\n", argv[0]);

return (0);
}
